pipeline {
    agent any

    stages {
        stage('build') {
            agent{
                docker{
                    image 'node:18-alpine'
                    reuseNOde true

                }
            }
            steps {
                sh '''
                ls -la
                node --version
                npm --version
                '''
            }
        }
    }
}